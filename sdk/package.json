{
  "name": "atlas-sdk-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Optional headless pipeline runner for the Atlas team, built on the Cursor SDK.",
  "type": "module",
  "scripts": {
    "pipeline": "tsx orchestrator.ts",
    "runner": "tsx runner.ts",
    "kickoff": "tsx kickoff-via-cc.ts",
    "test": "node --import tsx --test workspace-path.test.ts models.test.ts kickoff-via-cc.test.ts"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.18"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  },
  "overrides": {
    "tar": "^7.5.11",
    "undici": "^6.24.0"
  }
}

# Proposed lessons (awaiting approval)

Roles append new, non-obvious lessons here. The user approves in batches; approved items move to `lessons.md`. No secrets or PII.

Format:
```
- [ ] <lesson>. (role: <who>, rationale: <why>, source: <where>, date: <when>)
```

- [ ] Repo `knowledge/` now ships core-values-charter, enterprise-org-model, atlas-framework, and retro-template alongside installed KB; update install manifest if operators expect them under `~/.cursor/atlas-knowledge/`. (role: atlas-docs, rationale: four new canonical docs in repo knowledge/, source: knowledge/*.md creation, date: 2026-06-13)
- [ ] Control Center SDK integration: keep CC keyless; run Cursor SDK on the Windows host (not inside the CC container) when using Docker Desktop bind mounts and local runtime. (role: atlas-architect, rationale: local SDK needs host Cursor; container cannot invoke Windows CLI; avoids CURSOR_API_KEY in CC, source: run 2026-06-14-control-center-sdk design, date: 2026-06-14)
- [ ] SDK ingest mapping lives in `atlas-control-center/sdk/ingest-client.ts`; `cc-client.postIngest` and runner stream loops should delegate there so `run.stream()` → `IngestEvent` stays single-sourced. (role: atlas-ai-eng, rationale: avoids duplicate redaction/mapping logic between orchestrator and runner, source: run 2026-06-14-control-center-sdk ai-eng.md, date: 2026-06-14)
- [ ] Control Center Complete Reference drifts quickly after SDK runner work: verify §7 API, §11 SSE (`ingest` on events channel), §13 compose (named volumes + override), and §2 `examples.archived/` before publishing operator docs. (role: atlas-qa, rationale: reference still described stub subtasks and examples mounts after Phase 0–1, source: run 2026-06-14-control-center-sdk verification.md, date: 2026-06-14)
- [ ] Control Center README quick start should always document override.yml before first `docker compose up`; default named volumes start empty. (role: atlas-docs, rationale: operators hit empty UI without override, source: run 2026-06-14-control-center-sdk docs cleanup, date: 2026-06-15)
- [ ] atlas-lead runs that skip kickoff (flat deliverables at run root, no `gates/`) need server-side gate sync; meta `phase: complete` alone must not drive the Complete badge without `final` approved. (role: atlas-dev, rationale: legacy layout left Gates tab empty while run looked done, source: run 2026-06-14-control-center-sdk gates-sync, date: 2026-06-15)
- [ ] Control Center chokidar on Docker Desktop Windows bind mounts needs `usePolling: true` (or equivalent); native fs events do not reach the container, so Stream gets no `kind: file` activity and `/api/events/stream` `fs` never fires. (role: atlas-qa, rationale: integration test reproduced empty Stream after disk edits; in-container chokidar probe got zero events, source: run 2026-06-14-control-center-sdk integration-test-report.md, date: 2026-06-15)
- [ ] UI verification must curl `GET /api/runs/<id>/team` for `completed` count and `done` status after every CC deploy; disk `team.json` uses `completed` and `modelTier`, not API `done`/`tier`. Playwright `networkidle` hangs on SSE routes; use `domcontentloaded` per route. (role: atlas-qa, rationale: stale process returned `completed:0` until reload; full-page smoke timed out, source: run 2026-06-14-control-center-sdk ui-verification-report.md, date: 2026-06-15)
- [ ] Control Center Team page: keep run delegation (`team.json` via `/api/runs/:id/team`) separate from static role catalog (`agents/atlas-*.md` via `/api/roles/*`); do not merge into one endpoint. (role: atlas-architect, rationale: two sources of truth with different lifecycles and write targets, source: run 2026-06-14-control-center-sdk team-catalog-design.md, date: 2026-06-15)
- [ ] atlas-lead must set Task `description` explicitly to `atlas-<role>: <action>`; omitting it shows task-only titles in the UI with no member name. (role: atlas-lead, rationale: repeated charter violations noted in user feedback, source: user feedback 2026-06-15, date: 2026-06-15)
- [ ] SDK orchestrator and runner live in `atlas-control-center/sdk/`, not the Atlas install bundle; link to CC from atlas docs and keep Compose build context `./sdk`. (role: atlas-docs, rationale: SDK is part of the Control Center product, source: SDK colocation move, date: 2026-07-10)

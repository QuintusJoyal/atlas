# Operator data (never in repo)
.atlas/

# Host node_modules — npm is contained in Docker images only
**/node_modules/
sdk/dist/

# Local Cursor IDE config (operator-specific)
.cursor/

docker-compose.override.yml

*.log
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
